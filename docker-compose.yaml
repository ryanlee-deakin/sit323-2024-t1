version: '3'
services:
  app:
    build: .
    volumes:
      - .:/user/src/app
    container_name: docker_calculator_app
    restart: always
    ports:
      - 8080:3040
    command: bash -c "npm run start"